{
  "name": "e-invoiceza",
  "private": true,
  "version": "0.1.0",
  "description": "Multi-tenant EU e-invoicing SaaS platform",
  "scripts": {
    "dev": "pnpm --filter server dev",
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.0.0"
}
